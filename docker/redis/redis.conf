bind 0.0.0.0

protected-mode no

# Persistência via AOF
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec

# Sem snapshots RDB em desenvolvimento
save ""

# Gerenciamento de memória
maxmemory-policy allkeys-lru

# Logging para stdout do container
loglevel notice
logfile ""
